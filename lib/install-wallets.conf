# All Wallets that are  available and are installed with -a
#ALL_WALLETS="ORE ALTCOM"

# Default-User's UID
# default 1000
#USER_UID=1000

# Default-User's HOME
# uncomment this, if you changed USER_UID
#USER_HOME=`grep $USER_UID /etc/password|cut -d: -f6`

# Mountpoint of the BlockChain-Data HDD/Partition
# default /mnt/.btrfs/blkchn
#BLKCHN_VOL_MNT=/mnt/.btrfs/blkchn

# btrfs-mount Options of the former Partition
# defaults: "defaults,noatime,compress=lzo,autodefrag"
#BTRFS_MNT_OPTS="defaults,noatime,compress=lzo,autodefrag"

# Project Dir
# default $HOME/trustOne
#PRJKT_DIR=$HOME/trustOne
